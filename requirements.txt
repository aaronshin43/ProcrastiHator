# --- LiveKit Core & Agents ---
livekit>=0.17.0
livekit-agents>=0.12.0

# --- LiveKit Plugins (AI Models) ---
livekit-plugins-openai         # STT (Whisper)
google-genai                   # LLM (Gemini)
livekit-plugins-elevenlabs     # TTS (목소리)
livekit-plugins-silero         # VAD (말 끊김 감지)

# --- Client App (GUI) ---
PyQt6>=6.7.0

# --- Vision & Sensing ---
opencv-python                  # Webcam capture
mediapipe                      
numpy                          


pywin32; sys_platform == 'win32'
psutil                         # 프로세스 모니터링
python-dotenv                  # .env 파일 로드
colorlog                       # 로그 예쁘게 찍기
keyboard                       # global binding
sounddevice                    # sound play