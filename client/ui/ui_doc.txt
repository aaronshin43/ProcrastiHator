================================================================================
                    ProcrastiHator UI 문서 (main_window.py)
================================================================================

[프로젝트 개요]
PyQt6 기반의 음성 및 성격 선택 UI 애플리케이션입니다. 사용자는 먼저 음성을 선택한 후,
선택한 음성에 맞는 성격을 선택하는 2단계 선택 프로세스를 제공합니다.

================================================================================
                              클래스 구조
================================================================================

1. BaseCard (QFrame 상속)
   └─ 공통 카드 위젯 베이스 클래스
   └─ PersonalityCard와 VoiceCard의 공통 기능 제공

2. PersonalityCard (BaseCard 상속)
   └─ 성격 선택 카드 위젯
   └─ 아이콘, 제목, 설명을 표시

3. VoiceCard (BaseCard 상속)
   └─ 음성 선택 카드 위젯
   └─ 고정 아이콘(🔊), 제목만 표시

4. MainWindow (QMainWindow 상속)
   └─ 메인 윈도우 및 화면 관리
   └─ Voice 섹션과 Personality 섹션을 관리

================================================================================
                        네비게이션 로직 (Navigation & Routing)
================================================================================

[화면 구조]
┌─────────────────────────────────────────────────────────────┐
│                      MainWindow                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │         voice_section_widget (초기 화면)              │  │
│  │  ┌──────────────────────────────────────────────┐    │  │
│  │  │  Header: "Voice"                            │    │  │
│  │  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐               │    │  │
│  │  │  │VC1 │ │VC2 │ │VC3 │ │VC4 │ (4열 그리드)   │    │  │
│  │  │  └────┘ └────┘ └────┘ └────┘               │    │  │
│  │  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐               │    │  │
│  │  │  │VC5 │ │VC6 │ │VC7 │ │VC8 │               │    │  │
│  │  │  └────┘ └────┘ └────┘ └────┘               │    │  │
│  │  └──────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │    personality_section_widget (초기: 숨김)          │  │
│  │  ┌──────────────────────────────────────────────┐  │  │
│  │  │  Header: "Personality"                        │  │  │
│  │  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐                │  │  │
│  │  │  │PC1 │ │PC2 │ │PC3 │ │PC4 │ (4열 그리드)    │  │  │
│  │  │  └────┘ └────┘ └────┘ └────┘                │  │  │
│  │  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐                │  │  │
│  │  │  │PC5 │ │PC6 │ │PC7 │ │PC8 │                │  │  │
│  │  │  └────┘ └────┘ └────┘ └────┘                │  │  │
│  │  └──────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘

[화면 전환 흐름]

1. 초기 상태
   ┌─────────────────┐
   │ Voice 섹션      │ ← setVisible(True)
   │ Personality 섹션│ ← setVisible(False)
   └─────────────────┘

2. Voice 카드 클릭

   VoiceCard 클릭
      ↓
   clicked 시그널 발생
      ↓
   VoiceCard.on_card_clicked()
      - name.user_voice = title 저장
      ↓
   MainWindow.handle_voice_card_click()
      - click
      - voice_section_widget.setVisible(False)
      - personality_section_widget.setVisible(True)

3. Personality 화면 표시
   ┌─────────────────┐
   │ Voice 섹션      │ ← setVisible(False)
   │ Personality 섹션│ ← setVisible(True)
   └─────────────────┘

4. Personality 카드 클릭
   PersonalityCard 클릭
      ↓
   clicked 시그널 발생
      ↓
   PersonalityCard.on_card_clicked()
      - name.user_personality = title 저장
      ↓
   MainWindow.handle_personality_card_click()
      - 선택 상태 업데이트

[화면 전환 메커니즘]
- 방식: 위젯 가시성 제어 (setVisible)
- 두 섹션 모두 메모리에 생성되어 있음
- 전환 시 재생성 없이 가시성만 변경
- 장점: 빠른 전환, 상태 유지 가능

[레이아웃 정렬]
- 메인 레이아웃: 중앙 정렬 (AlignCenter)
- 섹션 위젯: 메인 레이아웃에 중앙 정렬로 추가
- 섹션 레이아웃: 중앙 정렬 (AlignCenter)
- Header: 섹션 레이아웃에 중앙 정렬로 추가
- Card 그리드: 섹션 레이아웃에 중앙 정렬로 추가
- 창 크기 고정 시에도 모든 요소가 중앙에 정렬됨

================================================================================
                          데이터 흐름 (Data Flow)
================================================================================

[데이터 소스]
- name.py 모듈에서 데이터 가져오기
  * personality_cards: [(icon, title, desc), ...] 형태의 튜플 리스트
  * voice_data: [title, ...] 형태의 문자열 리스트

[데이터 저장 위치]
- name.user_voice: 선택된 음성 이름 (VoiceCard 클릭 시 저장)
- name.user_personality: 선택된 성격 이름 (PersonalityCard 클릭 시 저장)
- name.user_nickname: 사용자 닉네임 (기본값: "user")

[데이터 흐름도]

1. Voice 선택 흐름
   name.voice_data
      ↓
   MainWindow.create_voice_section()
      ↓
   VoiceCard 생성
      ↓
   사용자 클릭
      ↓
   name.user_voice = title 저장

2. Personality 선택 흐름
   name.personality_cards
      ↓
   MainWindow.create_personality_section()
      ↓
   PersonalityCard 생성
      ↓
   사용자 클릭
      ↓
   name.user_personality = title 저장

[전역 변수 접근]
- 현재 구현: 직접 접근 (name.user_voice, name.user_personality)
- 저장 위치: name.py 모듈의 전역 변수
- 접근 방식: import name 후 name.변수명으로 접근

================================================================================
                          이벤트 흐름 (Event Flow)
================================================================================

[클릭 이벤트 처리 흐름]

VoiceCard 클릭:
   사용자 클릭
      ↓
   BaseCard.mousePressEvent()
      - clicked 시그널 발생
      - on_card_clicked() 호출
      ↓
   VoiceCard.on_card_clicked()
      - name.user_voice = title 저장
      ↓
   MainWindow.handle_voice_card_click()
      - 선택 상태 업데이트
      - 화면 전환 (Voice → Personality)

PersonalityCard 클릭:
   사용자 클릭
      ↓
   BaseCard.mousePressEvent()
      - clicked 시그널 발생
      - on_card_clicked() 호출
      ↓
   PersonalityCard.on_card_clicked()
      - name.user_personality = title 저장
      ↓
   MainWindow.handle_personality_card_click()
      - 선택 상태 업데이트

[시그널-슬롯 연결]
- VoiceCard.clicked → MainWindow.handle_voice_card_click
- PersonalityCard.clicked → MainWindow.handle_personality_card_click
- 연결 시점: 카드 생성 시

================================================================================
                        주요 상수 및 설정값
================================================================================

[윈도우 설정]
- 창 크기: 1000 x 600
- 창 위치: (100, 100)
- 배경색: #121212

[레이아웃 설정]
- 카드 간 간격: 15px
- 그리드 열 수: 4열
- 섹션 제목 폰트: 22px, bold, white
- 메인 레이아웃 정렬: 중앙 정렬 (AlignCenter)
- 섹션 레이아웃 정렬: 중앙 정렬 (AlignCenter)
- Header 및 Card 그리드: 중앙 정렬로 배치

[카드 크기]
- VoiceCard: 156 x 110
- PersonalityCard: 160 x 192

[카드 마진]
- BaseCard 기본: (10, 30, 10, 10)  [좌, 상, 우, 하]
- VoiceCard: (10, 10, 10, 10)

[스타일 색상]
- 선택됨 배경: #2A1A1C
- 선택됨 테두리: #D64550
- 기본 배경: #1A1B1E
- 기본 테두리: #333333
- 호버 테두리: #555555
- 호버 배경: #252629
- 제목 색상: #E0E0E0
- 설명 색상: #A0A0A0

[폰트 크기]
- 아이콘: 32px
- 제목: 16px, bold
- 설명: 13px

================================================================================
                        변수 가져오기
================================================================================

name.py 에서 voice_data 와 personality_cards 를 가져옵니다.

================================================================================
                          확장 가능성
================================================================================

[추가 가능한 기능]
2. 완료 버튼: Personality 선택 후 최종 확인
3. 애니메이션: 화면 전환 시 페이드 효과
4. 데이터 검증: 선택된 값이 유효한지 확인
5. 설정 저장: 선택한 값을 파일에 저장

[코드 개선 가능 영역]
1. 전역 변수 접근: 시그널로 데이터 전달하도록 개선
2. 하드코딩된 값: 상수로 분리

================================================================================
                              끝
================================================================================
